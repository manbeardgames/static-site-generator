/*
    index.ts is your main entry point, unless you configured somethign differently
    in the pacakage.json configuration
*/

/*
    This is just an example. Don't use this.  Delete it.
    Or keep it. Up to you
*/

export const Hello = (name: string) => `Hello ${name}`;
